---
name: llm-nodes
description: When implementing novel generation pipeline, use director-chapter-role architecture with 6 unit types (narrator/environment/action/dialogue/psychology/conflict), supporting mock mode and multi-layer JSON parsing
version: 1.0.0
tags: [llm-nodes, novel-generation, director-chapter-role, unit-types, mock-mode]
---

# LLM 节点

## 目标
实现小说生成的多智能体流水线，包括总导演规划、章节导演、角色分配、角色扮演、自检和文本润色六个核心节点。

## 触发条件
- 需要生成小说整体规划时 → director_general
- 需要生成章节节点序列时 → director_chapter
- 需要为节点分配角色和生成提示时 → role_assigner
- 需要执行角色扮演生成内容时 → role_actor
- 需要审查内容质量时 → self_check
- 需要润色章节文本时 → text_polisher

## 前置检查
1. LLM 客户端已初始化
2. 输入数据符合 Pydantic Schema
3. MOCK_MODE 设置正确（开发/生产环境）

## 节点架构

### 节点关系图

```
director_general (总导演)
    ↓
director_chapter (章节导演)
    ↓
role_assigner (角色分配器)
    ↓
role_actor (角色扮演)
    ↓
self_check (自检/审查)
    ↓ (循环或继续)
text_polisher (文本润色) [章节结束]
```

## 核心节点

### 1. director_general - 总导演

**职责**：生成小说整体规划

**输入**：DirectorGeneralInput
- theme, style, total_words, character_count, genre

**输出**：DirectorGeneralOutput
- world_building, writing_style, outline, chapter_count
- characters, character_names, character_cards
- conflict_design, foreshadowing, character_arcs

**关键特性**：
- 支持 5 种文体：novel/script/game_story/dialogue/article
- 生成详细的角色卡片（包含性格、习惯、优缺点、恐惧、秘密）
- MOCK_MODE 下返回预定义数据

**使用示例**：
```python
plan = director_general(DirectorGeneralInput(
    theme="科幻小说",
    style="硬科幻",
    total_words=50000,
    character_count=3,
    genre="novel"
))
```

### 2. director_chapter - 章节导演

**职责**：生成章节节点序列

**输入**：DirectorChapterInput
- chapter_id, director_general_output, global_memory_snapshot
- genre, feedback（上一轮审查反馈）

**输出**：DirectorChapterOutput
- chapter_outline（章节大纲）
- node_sequence（节点序列，包含6种单元类型）
- node_count, character_presence_plan

**6种单元类型**：

| 类型 | 职责 | 示例 |
|------|------|------|
| narrator | 旁白叙事，推进时间线 | "三天后，城里的局势变得更加紧张" |
| environment | 环境描写，营造氛围 | "雨后的青石板路泛着湿润的光泽" |
| action | 动作描写，刻画外在 | "他皱起眉头，右手不自觉地摩挲着茶杯边缘" |
| dialogue | 角色对话，推动情节 | "『我们真的没有别的选择了吗？』她低声问道" |
| psychology | 心理描写，刻画内心 | "她心里涌起一股不安，那个念头挥之不去" |
| conflict | 冲突悬念，制造张力 | "『站住别动！』一个低沉的声音从黑暗中传来" |

**重要规则**：
- 必须使用固定角色姓名（来自 character_names）
- 必须遵循角色卡片设定
- node_sequence 必须包含所有6种单元类型

### 3. role_assigner - 角色分配器

**职责**：为当前节点生成角色扮演提示

**输入**：RoleAssignerInput
- current_node（节点信息：type, description, target_character）
- character_profile（角色档案）
- character_names, character_cards（所有角色信息）
- generated_summaries（已生成内容摘要）
- feedback（审查反馈）

**输出**：RoleAssignerOutput
- target_character（目标角色）
- generation_prompt（PromptComponents）
- rag_queries（RAG 检索语句列表）

**单元类型特定提示**：

根据 node_type 生成不同的 identity 和 expected_reaction：

```python
unit_type_prompts = {
    "narrator": "你是旁白叙事者，以第三人称叙述故事",
    "environment": "你是环境描写专家，以第三人称描写场景",
    "action": "你是动作描写专家，描写角色的外在表现",
    "dialogue": "你是XXX，[角色详细信息]",
    "psychology": "你是心理描写专家，刻画角色的内心世界",
    "conflict": "你是冲突悬念大师，制造故事张力"
}
```

**RAG 检索**：
- 生成最多 3 个检索语句
- 用于查找角色历史记忆
- 例如："角色A在书房思考"、"角色B与角色C的对话"

### 4. role_actor - 角色扮演

**职责**：执行实际的内容生成

**输入**：RoleAssignerOutput + 其他参数
- chapter_id, node_id, node_type
- feedback（审查反馈）
- stream_callback（流式回调）
- user_theme, user_style 等用户信息

**输出**：RoleActorOutput
- generated_content（生成的正文内容）
- state_change_report（状态变更报告）

**MOCK_MODE 行为**：
```python
if MOCK_MODE:
    content = f"[第{chapter_id}章 - {node_id}] 模拟生成内容..."
    if feedback:
        content = f"[根据审查反馈修改]{feedback}..."
    return {"generated_content": content, "state_change_report": {"summary": ""}}
```

**单元类型指令**：
根据 node_type 添加不同的系统指令：

```python
unit_type_instructions = {
    "narrator": "你是一个旁白叙事者。以第三人称叙述故事...",
    "environment": "你是一个环境描写专家。以第三人称描写场景...",
    "action": "你是一个动作描写专家。描写角色的外在表现...",
    "dialogue": "你是一个角色扮演AI。不要输出任何JSON...",
    "psychology": "你是一个心理描写专家。描写角色的内心世界...",
    "conflict": "你是一个冲突悬念大师。制造故事张力...",
}
```

**输出验证**：
- 使用 `_validate_output()` 检查内容长度 >= 50
- 检测内容是否被截断（不以 `}` 或 `"]` 结尾）
- 失败时自动重试（最多 3 次）

### 5. self_check - 自检/审查

**职责**：审查生成内容是否符合标准

**输入**：
- director_general_standards（总导演标准）
- current_chapter_content（当前章节内容）
- summary（全局记忆）
- previous_feedback（上一轮反馈）
- chapter_id, node_id

**输出**：SelfCheckOutput
- needs_revision（是否需要修订）
- issue_types（问题类型列表）
- specific_issues（具体问题描述）
- improvement_suggestions（改进建议）

**审查维度**：
1. 内容完整性：是否完整、有无被截断
2. 角色一致性：行为是否与设定一致
3. 情节符合度：是否符合章节大纲
4. 记忆一致性：关系变化是否与记忆一致
5. 情感合理性：情感转折是否合理

**MOCK_MODE 行为**：
- 50% 概率通过
- 50% 概率随机返回一种问题类型（角色/情节/情感/场景/对话）

### 6. text_polisher - 文本润色

**职责**：对完成的章节进行文本润色

**输入**：TextPolisherInput
- chapter_text（章节文本）

**输出**：TextPolisherOutput
- polished_text（润色后的文本）

**润色要求**：
1. 保持原文核心意思和情节不变
2. 优化语言表达，使其流畅自然
3. 增强描写细节，提升画面感
4. 修正语法错误和不通顺的表达
5. 保持原有文体风格
6. 删除啰嗦部分
7. 添加细节使衔接更自然
8. 保持物体名称一致性

## 辅助功能

### JSON 多层解析策略

`_parse_state_change_report()` 实现多层回退解析：

```
策略A：直接解析整个内容
    ↓ 失败
策略B：首尾清理后解析
    ↓ 失败
策略C：括号匹配提取
    ↓ 失败
策略D：智能括号匹配（处理嵌套）
    ↓ 失败
策略E：代码块内 JSON 提取
    ↓ 失败
策略F：正则表达式直接提取字段
    ↓ 失败
回退：返回原始内容作为 content
```

### MOCK_MODE 机制

**用途**：开发和测试时无需调用真实 API

**启用方式**：
```python
# config.yaml
generation:
  mock_mode: true
```

**影响节点**：
- director_general：返回 MOCK_DIRECTOR_GENERAL
- director_chapter：返回模拟数据
- role_assigner：返回模拟角色分配
- role_actor：返回模拟生成内容
- self_check：随机通过/失败
- text_polisher：返回模拟润色文本

## 边界与约束
- **MOCK_MODE**：所有节点支持模拟模式，便于开发和测试
- **JSON 解析**：多层回退策略确保最大程度提取有效内容
- **重试机制**：role_actor 内部实现最多 3 次重试
- **流式输出**：role_actor 和 text_polisher 支持流式回调
- **Schema 验证**：关键节点使用 @validate_schema 装饰器

## 验证清单
- [ ] MOCK_MODE 设置正确
- [ ] 输入数据符合 Pydantic Schema
- [ ] LLM 客户端已初始化
- [ ] 角色姓名使用固定列表中的名称
- [ ] 节点序列包含所有6种单元类型
- [ ] 审查反馈正确传递给下一轮
- [ ] 流式回调函数不会阻塞
- [ ] JSON 解析失败时有适当的回退处理
