---
name: novel-architect
description: When building a novel generation system, use multi-agent pipeline with director-chapter-role architecture, supporting sliding window review and human intervention
version: 2.0.0
tags: [novel-generation, multi-agent, fastapi, websocket]
---

# 小说生成架构规范

## 目标
构建一个多智能体协作的小说生成系统，支持实时流式输出、滑动窗口审查和人工干预。

## 触发条件
- 用户通过 API 启动小说生成任务
- 需要理解系统架构和模块关系时
- 调试或扩展系统功能时

## 前置检查
1. Python 3.8+ 环境
2. FastAPI 和 WebSocket 支持
3. Kimi API 密钥配置（config.yaml）
4. 输出目录可写权限

## 系统架构

### 核心模块

| 模块 | 职责 | 关键类/函数 |
|------|------|------------|
| main.py | FastAPI 入口、生成循环、状态管理 | generate_task(), generation_state |
| llm_nodes.py | LLM 节点实现 | director_general, director_chapter, role_assigner, role_actor, self_check |
| schemas.py | Pydantic 数据模型 | DirectorGeneralInput/Output, RoleAssignerInput/Output, etc. |
| llm_client.py | LLM 客户端 | get_llm_client(), LlmClient |
| context_managers.py | 章节上下文管理 | ChapterContext, load/save_global_memory |
| iterators.py | 节点和章节迭代 | NodeSequence, ChapterIterator |
| memory_store.py | 记忆检索 | memory_retriever, update_memory |
| rag_memory.py | RAG 向量存储 | add_chunks(), search() |
| observability.py | 运行时追踪 | Observability, start_span() |
| decorators.py | 装饰器 | json_output, validate_schema |

### 前端模块 (novel_ai/)

| 模块 | 职责 |
|------|------|
| src/App.tsx | React 主应用 |
| src/api/client.ts | API 客户端和 WebSocket |
| src/stores/appStore.ts | Zustand 状态管理 |
| src/components/ | UI 组件（ChatPanel, ProgressPanel, etc.） |
| src-tauri/ | Tauri Rust 后端 |

## 执行流程

### 步骤 1：初始化
- **动作**：加载配置、初始化 LLM 客户端、设置观察性
- **代码**：
```python
def init_app():
    global config, observability, DEBUG_MODE
    config = load_config()
    apply_mock_mode(config)
    DEBUG_MODE = config.get("generation", {}).get("debug", False)
    _ = get_llm_client()
    observability = get_observability()
```

### 步骤 2：总导演规划
- **动作**：生成小说整体规划
- **输入**：theme, style, total_words, character_count, genre
- **输出**：chapter_count, chapter_outlines, character_cards, world_building
- **工具**：`director_general(DirectorGeneralInput)`

### 步骤 3：章节循环
- **动作**：遍历每个章节
- **迭代器**：`ChapterIterator(1, chapter_count + 1)`
- **上下文**：`ChapterContext(i, "config.yaml", "global_memory.json")`

### 步骤 4：章节导演
- **动作**：生成章节节点序列
- **输入**：chapter_id, director_general_output, global_memory_snapshot
- **输出**：node_sequence（节点列表）
- **工具**：`director_chapter(DirectorChapterInput)`

### 步骤 5：节点执行循环
- **动作**：遍历节点序列，执行角色分配和角色扮演
- **迭代器**：`NodeSequence(node_sequence)`
- **流程**：
  1. 角色分配：`role_assigner(RoleAssignerInput)`
  2. RAG 检索：`search(query, top_k=3)`
  3. 角色扮演：`role_actor(role_assign_output_obj, ...)`
  4. 滑动窗口审查（见 sliding_window_review.skill）

### 步骤 6：章节润色
- **动作**：对完成的章节进行文本润色
- **工具**：`text_polisher(TextPolisherInput)`
- **输出**：polished_chapter

### 步骤 7：完成输出
- **动作**：保存最终文本到文件
- **输出目录**：`output/`
- **文件名**：`novel_YYYYMMDD_HHMMSS.txt`, `最终文本_YYYYMMDD_HHMMSS.txt`

## 关键数据结构

### generation_state（全局状态）
```python
{
    "is_running": bool,
    "is_paused": bool,
    "is_stopped": bool,
    "current_chapter": int,
    "current_node": str,
    "total_chapters": int,
    "novel_content": str,
    "output_file": str | None,
    "error": str | None,
    "chapter_feedback": str,
    "need_human_intervention": bool,
    "intervention_data": dict | None,
    "sliding_window": {
        "enabled": bool,
        "window_size": int,
        "window_start": int,
        "node_contents": dict,
        "prev_node_idx": int,
        "consecutive_failures": int,
        "retry_count": int,
    },
}
```

### API 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| /api/start | POST | 启动生成任务 |
| /api/status | GET | 获取当前状态 |
| /api/pause | POST | 暂停生成 |
| /api/resume | POST | 恢复生成 |
| /api/stop | POST | 停止生成 |
| /api/select_version | POST | 选择版本（人工干预） |
| /api/retry_node | POST | 重试当前节点 |
| /api/stream | WebSocket | 实时流式通信 |

## 边界与约束
- **并发**：单任务模式，不支持并发生成
- **状态**：状态存储在内存中，重启丢失
- **文件**：所有输出使用 utf-8 编码
- **API**：依赖 Moonshot API，需要有效 API 密钥
- **RAG**：使用简单向量存储，非专业向量数据库

## 配置规范 (config.yaml)

```yaml
api:
  provider: moonshot
  base_url: "https://api.moonshot.cn/v1"
  api_key: "${API_KEY}"  # 从环境变量读取
  model: "kimi-k2.5"
  max_retries: 3
  timeout: 300

generation:
  temperature: 0.7
  top_p: 0.9
  max_tokens: 4000
  mock_mode: false  # 模拟模式（用于测试）
  debug: false      # 调试日志
```

## 验证清单
- [ ] 所有模块能正确导入
- [ ] config.yaml 格式正确
- [ ] API 密钥配置正确
- [ ] 输出目录可写
- [ ] WebSocket 连接正常
- [ ] 滑动窗口审查正常工作
- [ ] 人工干预流程可正常触发和处理
