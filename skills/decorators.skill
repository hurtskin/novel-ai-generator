---
name: decorators
description: When processing LLM node outputs, use json_output and validate_schema decorators to parse JSON responses and validate against Pydantic schemas with automatic retry
version: 1.0.0
tags: [json-parsing, schema-validation, pydantic, decorators]
---

# 装饰器工具

## 目标
提供统一的 LLM 输出解析和 Schema 验证机制，处理各种格式的 JSON 响应，确保数据符合预期结构。

## 触发条件
- LLM 节点返回需要解析的 JSON 字符串
- 需要验证输出是否符合 Pydantic Schema
- 处理 markdown 代码块包裹的 JSON

## 前置检查
1. Pydantic 已安装
2. 目标 Schema 类继承自 `BaseModel`
3. LLM 输出包含可解析的 JSON

## 装饰器说明

### 1. json_output 装饰器

**用途**：将函数输出统一转换为 Python 字典

**处理流程**：
```python
@json_output
def llm_node(...) -> dict:
    # 原始输出可能是：
    # 1. Pydantic 模型
    # 2. JSON 字符串（可能包裹在 markdown 代码块中）
    # 3. 其他类型
    return raw_output
```

**转换规则**：

| 输入类型 | 处理方式 | 示例 |
|---------|---------|------|
| Pydantic 模型 | 调用 `model_dump()` | `BaseModel` → `dict` |
| JSON 字符串 | 清理后 `json.loads()` | `'{"key": "value"}'` → `dict` |
| Markdown JSON | 去除代码块标记 | `` ```json {...} ``` `` → `dict` |
| 带引号字符串 | 去除外层引号 | `'"{"key": "value"}"'` → `dict` |
| 其他类型 | 原样返回 | `any` → `any` |

**清理逻辑**：
```python
# 去除 markdown 代码块标记
re.sub(r'^```json\s*', '', json_str, flags=re.IGNORECASE)
re.sub(r'^```\s*', '', json_str)
re.sub(r'\s*```$', '', json_str)

# 去除外层引号
if json_str.startswith("'") and json_str.endswith("'"):
    json_str = json_str[1:-1]
if json_str.startswith('"') and json_str.endswith('"'):
    json_str = json_str[1:-1]

# 提取 properties（如果存在嵌套）
if isinstance(parsed, dict) and "properties" in parsed:
    parsed = parsed.get("properties", parsed)
```

**错误处理**：
- JSON 解析失败时，尝试从文本中提取 `{}` 包裹的内容
- 仍失败时返回 `{"_raw": raw_str, "_parse_error": error_msg}`

### 2. validate_schema 装饰器

**用途**：验证输出是否符合指定的 Pydantic Schema

**基本用法**：
```python
@validate_schema(RoleActorOutput)
def role_actor(...) -> dict:
    # 返回原始输出
    return raw_output
```

**带重试参数**：
```python
@validate_schema(DirectorGeneralOutput, max_retries=3)
def director_general(...) -> dict:
    return raw_output
```

**执行流程**：
```
调用函数获取结果
    ↓
尝试解析为字典（同 json_output 逻辑）
    ↓
Schema 特定字段修复（如 genre_specific）
    ↓
Pydantic model_validate() 验证
    ↓
[验证通过] → 返回 model_dump()
[验证失败] → 重试（最多 max_retries 次）
    ↓
超过最大重试次数 → 抛出 SchemaValidationError
```

**特殊字段处理**：

对于 `DirectorGeneralOutput`：
```python
if "genre_specific" in result:
    gs = result["genre_specific"]
    if isinstance(gs, str):
        # 字符串转为标准格式
        result["genre_specific"] = {
            "genre": "novel",
            "specific_fields": {"description": gs}
        }
    elif isinstance(gs, dict) and "genre" not in gs:
        result["genre_specific"] = {
            "genre": "novel",
            "specific_fields": gs
        }
```

对于 `DirectorChapterOutput`：
```python
# 处理 character_presence_plan 嵌套结构
if "character_presence_plan" in result:
    cpp = result["character_presence_plan"]
    if isinstance(cpp, dict):
        for key, value in cpp.items():
            if isinstance(value, dict) and "scenes" in value:
                cpp[key] = value["scenes"]

# 同样处理 genre_specific
```

## 组合使用

**推荐模式**：
```python
@json_output
@validate_schema(RoleActorOutput, max_retries=3)
def role_actor(
    role_assigner_output: RoleAssignerOutput,
    chapter_id: int,
    node_id: int,
    ...
) -> Dict[str, Any]:
    """
    角色扮演 LLM 节点
    
    装饰器执行顺序（从内到外）：
    1. validate_schema: 验证并返回 dict
    2. json_output: 确保最终输出为 dict
    """
    # 调用 LLM 获取原始响应
    response = client.chat_completion(...)
    return response
```

## 异常处理

### SchemaValidationError
```python
class SchemaValidationError(Exception):
    def __init__(self, errors: list):
        self.errors = errors
        super().__init__(f"Schema validation failed: {errors}")
```

**捕获示例**：
```python
try:
    result = role_actor(...)
except SchemaValidationError as e:
    logging.error(f"Validation failed: {e.errors}")
    # 处理验证错误
```

## 边界与约束
- **重试次数**：默认 3 次，可在装饰器参数中调整
- **字段修复**：仅对特定 Schema（DirectorGeneralOutput、DirectorChapterOutput）自动修复
- **日志**：使用 `logging.warning` 记录调试信息
- **性能**：每次重试都会重新调用被装饰函数

## 验证清单
- [ ] 目标 Schema 继承自 `pydantic.BaseModel`
- [ ] 装饰器顺序正确（validate_schema 在内层，json_output 在外层）
- [ ] max_retries 设置合理（考虑 API 调用成本）
- [ ] 捕获并处理 SchemaValidationError
- [ ] 检查日志输出确认解析过程正常
