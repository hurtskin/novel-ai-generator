---
name: sliding-window-review
description: When reviewing novel generation output, use sliding window mechanism to check content quality node-by-node with version management and human intervention support
version: 1.0.0
tags: [novel-generation, quality-control, review-system]
---

# 滑动窗口审查系统

## 目标
实现逐节点的内容质量审查，支持多版本管理和人工干预，确保生成内容符合质量标准。

## 触发条件
- 每完成一个角色扮演节点（role_actor）后
- 用户请求审查当前生成内容时
- 节点重试次数超过阈值时

## 前置检查
1. `generation_state["sliding_window"]` 已初始化
2. `self_check` 函数可用（来自 llm_nodes.py）
3. 全局记忆加载正常

## 执行流程

### 步骤 1：初始化窗口状态
```python
# 在 generation_state 中维护滑动窗口状态
generation_state["sliding_window"] = {
    "enabled": True,           # 是否启用滑动窗口
    "window_size": 2,          # 窗口大小（当前实现为动态）
    "window_start": 0,         # 当前窗口起始位置
    "node_contents": {},       # 节点内容存储 {node_index: {versions: [], status: "pending"}}
    "prev_node_idx": -1,       # 前一个通过的节点索引
    "consecutive_failures": 0, # 连续失败次数
    "retry_count": 0,          # 当前重试次数
}
```

### 步骤 2：存储节点生成内容
- **动作**：将 role_actor 输出存储到滑动窗口
- **代码**：
```python
sw = generation_state["sliding_window"]
if current_index not in sw["node_contents"]:
    sw["node_contents"][current_index] = {"versions": [], "status": "pending"}
sw["node_contents"][current_index]["versions"].append(raw_content)

# 限制版本数量（防止内存溢出）
max_versions = config.get("api", {}).get("max_retries", 3) + 1
if len(sw["node_contents"][current_index]["versions"]) > max_versions:
    sw["node_contents"][current_index]["versions"].pop(0)
```
- **成功标准**：内容已存储，版本数量在限制内
- **失败处理**：记录错误日志，尝试继续执行

### 步骤 3：构建审查窗口内容
- **动作**：组合前一个节点和当前节点内容
- **代码**：
```python
prev_content = ""
if sw["prev_node_idx"] >= 0 and sw["prev_node_idx"] in sw["node_contents"]:
    if sw["node_contents"][sw["prev_node_idx"]]["versions"]:
        prev_content = sw["node_contents"][sw["prev_node_idx"]]["versions"][-1]

window_content = prev_content + "\n\n" + current_content if prev_content else current_content
```
- **成功标准**：成功构建包含上下文衔接的审查内容
- **失败处理**：如果前节点不存在，仅审查当前节点

### 步骤 4：执行质量审查
- **动作**：调用 self_check 进行质量审查
- **工具**：`self_check(plan, window_content, global_memory, feedback, chapter_id, node_id)`
- **成功标准**：返回包含 `needs_revision` 和 `improvement_suggestions` 的结果
- **失败处理**：如果 self_check 异常，默认标记为需要修订

### 步骤 5：处理审查结果

**通过（needs_revision = false）：**
```python
feedback = ""
generation_state["chapter_feedback"] = ""
sw["node_contents"][current_index]["status"] = "passed"
sw["prev_node_idx"] = current_index  # 更新前节点索引
sequence.retry_count = 0

# 保存到全局记忆和RAG
summary = role_actor_output.get("state_change_report", {}).get("summary", "")
if summary:
    global_memory.append(summary)
    save_global_memory(global_memory)

# 持久化到文件
chapter_content += raw_content + "\n\n"
from rag_memory import add_chunks
add_chunks([raw_content], {"chapter_id": i, "node_id": node_id})
```

**失败（needs_revision = true）：**
```python
improvement_suggestions = check_result.get("improvement_suggestions", "")
if improvement_suggestions:
    old_feedback = feedback or ""
    if old_feedback:
        feedback = old_feedback + "\n\n" + improvement_suggestions
    else:
        feedback = improvement_suggestions
    generation_state["chapter_feedback"] = feedback

# 触发重试
sequence.send(improvement_suggestions)
```

### 步骤 6：最大重试处理
- **条件**：`sequence.retry_count > max_retries`
- **动作**：触发人工干预
- **代码**：
```python
intervention_data = {
    "chapter": i,
    "node_index": current_index,
    "node_id": node_id,
    "versions": sw["node_contents"].get(current_index, {}).get("versions", []),
}

await broadcast_ws("need_manual_review", intervention_data)
generation_state["need_human_intervention"] = True
generation_state["intervention_data"] = intervention_data
generation_state["is_paused"] = True
```
- **成功标准**：前端收到人工审查请求，任务暂停
- **失败处理**：记录错误，尝试自动选择最新版本继续

### 步骤 7：人工干预处理
**用户选择重试：**
```python
sw["retry_current"] = True
generation_state["is_paused"] = False
sw["node_contents"][current_index]["versions"] = []
sequence.retry_count = 0
sequence.send(feedback)
```

**用户选择版本：**
```python
sw["selected_version_idx"] = version_index
generation_state["is_paused"] = False
raw_content = sw["node_contents"][current_index]["versions"][selected_version_idx]

# 标记通过并继续
sw["node_contents"][current_index]["status"] = "passed"
sw["prev_node_idx"] = current_index
chapter_content += raw_content + "\n\n"
```

## 边界与约束
- **窗口大小**：当前实现为动态（前节点+当前节点），非固定窗口
- **版本限制**：每个节点最大版本数 = max_retries + 1
- **不做**：不跨章节审查，不修改已通过节点
- **安全**：所有文件操作使用 utf-8 编码，避免字符问题

## 输出规范

### 节点状态
```python
{
    "versions": [str],      # 所有生成版本列表
    "status": "pending" | "passed"  # 节点审查状态
}
```

### 人工干预数据
```python
{
    "chapter": int,         # 当前章节
    "node_index": int,      # 节点索引
    "node_id": str,         # 节点ID
    "versions": [str],      # 可供选择的版本列表
}
```

### WebSocket 事件
- `need_manual_review`：需要人工审查
- `status`：状态更新（包含 need_human_intervention 标志）

## 验证清单
- [ ] 滑动窗口状态正确初始化
- [ ] 节点内容正确存储到 node_contents
- [ ] 版本数量不超过 max_versions 限制
- [ ] 审查时正确组合前节点和当前节点内容
- [ ] 通过时正确更新 prev_node_idx
- [ ] 失败时正确累积 feedback
- [ ] 达到 max_retries 时正确触发人工干预
- [ ] 人工干预后能正确恢复或重试
- [ ] 通过的节点内容正确保存到文件和RAG
